# API

API conventions, endpoint specifications, schemas, authentication, authorization, versioning, events, and integration contracts belong here.

Canonical OpenAPI baseline: [openapi.yaml](openapi.yaml). All endpoints use `/api/v1`, JSON, ISO-8601 timestamps, cursor pagination, structured errors, bearer auth, and `Idempotency-Key` for imports, calculations, and approval mutations.
