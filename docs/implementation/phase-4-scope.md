# Phase 4 — Change Management

The domain contract supports potential/pending/approved changes, weighted exposure (`estimated cost × probability`), and explicit incorporation eligibility. Only an approved change with a non-negative approved cost may update current budget; agents may recommend impact but cannot approve or incorporate changes.
