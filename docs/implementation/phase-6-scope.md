# Phase 6 — Agentic Intelligence

The domain contract now enforces evidence-linked findings, confidence between 0 and 1, explicit agent type, and human-only disposition (`ACCEPTED`, `DISMISSED`, `ESCALATED`). Agents are advisory and cannot mutate baseline, actual, forecast approvals, changes, or period locks. Runtime orchestration, persistence, rule engines, and finding UI remain integration work.
